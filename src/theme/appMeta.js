// App metadata kept in sync with app.json. Update both when bumping versions.
// Keeping this as a JS module avoids taking a hard runtime dependency on
// expo-constants (which is normally a transitive of expo but not listed
// in this project's package.json).
export const APP_VERSION = "1.2.0";
export const APP_NAME = "Tabu GO";
